apply plugin: 'java'
apply plugin: 'application'

mainClassName = "matrix.launch"

jar {
    baseName = "Matrix"
    version = "0.1.0"
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.5'
}
